{{!-- <button class='submit' id='logout' type='submit'>LOG OUT</button>

<script src='/js/logout.js'></script> --}}
<section>

    <div id='story'>
        <h4>{{story.content}}</h4>
    {{#each story.choices as |choice|}}
    <p>{{this.content}}</p>
    {{/each}}
    </div>

    <div id='image'><img src="/assets/{{story.filename}}"></div>

</section>

<script src='/js/logout.js'></script>